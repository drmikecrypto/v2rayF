Place Android geo assets here (not committed to git):

  geoip.dat
  geosite.dat

Cores are packaged separately under NativeLibs/arm64-v8a/:
  libxray.so
  libsingbox.so

Download Xray + sing-box via: pwsh -File scripts/package-android.ps1
