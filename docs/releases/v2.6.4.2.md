# v2.6.4.2

## Why

v2.6.4.1 field soak passed on Android and Windows. Phase C opens with a first slice: richer diagnostics and honest status — not a UI redesign.

## Added

- Settings → **Copy session diagnostics** — Connect → SOCKS → TUN → soft recovery / multipath timeline
- Stronger Daily vs Gaming Boost copy + recover CTAs when TUN/HTTP assist is weak
- Multipath flake status under Gaming (Soft Multipath re-ranks on reconnect; **Survive stays off**)
- Golden-matrix lab harness + tip docs (`docs/tips/phase-c.md`, `docs/tips/golden-matrix-2.6.4.1.md`)

## Verify

```powershell
dotnet test src/v2rayF.Core.Tests/v2rayF.Core.Tests.csproj -c Release --filter FullyQualifiedName~SessionDiagnostics|FullyQualifiedName~GoldenMatrix|FullyQualifiedName~GamingBoost
```

On device: Connect → Settings → Copy session diagnostics → paste into private notes. Trigger weak TUN or multipath path miss and confirm status CTAs (no silent Survive).
