# v2rayF v2.2.7

CI fix — ships the v2.2.6 Android sing-box TUN changes.

## Fixed

- Release workflow no longer fails on a PowerShell syntax error in `package-android.ps1`
- Use this build instead of the failed **v2.2.6** tag (no GitHub release assets were published)

## Includes (from 2.2.6)

- Removed invalid sing-box JSON field `file_descriptor`
- Patched `libsingbox.so` with `SING_BOX_TUN_FD` VPN fd support

## Install

Tap **Update** in the app when offered.
