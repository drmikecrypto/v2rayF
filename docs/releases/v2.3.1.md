# v2rayF v2.3.1

Fix Instagram Direct (TUN-only; no VPN HTTP proxy).

## Fixed

- WhatsApp/Telegram worked on TUN while **Instagram Direct** stayed offline — Meta MQTT was using VPN HTTP proxy `10809` (CONNECT fails)
- Android VPN no longer calls **`SetHttpProxy`**; all apps use TUN (FakeIP + gVisor)
- TUN sets **`sniff_override_destination: true`** for FakeIP / Meta TLS

## Install

Tap **Update**. Private DNS Off → Connect → **force-stop Instagram once** → open Direct.
