# v2rayF v2.5.0.2

WhatsApp + Instagram online on Android TUN (your Sentinel / V2Box-style configs).

## Fixed

- **WhatsApp** — TUN `sniff_override_destination: true` for FakeIP messaging
- **Instagram** — empty Meta HTTP proxy exclusion list so feed/HTTPS uses VPN `10809` like Google (no longer forced onto broken gVisor-only path)

Keeps 2.5.0.1 bootstrap DNS fix (no Connect hang).

## Install

Tap **Update**. Private DNS Off → Connect → force-stop Instagram and WhatsApp once. Confirm Google/Telegram still OK.
