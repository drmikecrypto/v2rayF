# v2rayF v2.2.1

Emergency reliability restore after 2.2.0 made Test delay / Connect time out on all configs.

## Fixed

- Android classic (VLESS / VMess / Trojan / SS / REALITY / Vision) back on **Xray** + VPN HTTP proxy `10809`
- Test delay uses **UDP DNS**; live Connect still defaults Secure DNS (DoH) with one-shot DoH retry
- sing-box ephemeral speedtest no longer also binds `10809` (parallel workers no longer clash)

Hy2 / TUIC / WireGuard / anytls still run on sing-box.

## Install

Uninstall old APK → install 2.2.1 → Private DNS Off → Packet fragment Off.
