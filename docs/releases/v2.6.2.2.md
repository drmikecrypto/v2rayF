# v2rayF v2.6.2.2

Reliability hotfix after v2.6.2.1 — false-green probes, VPN revoke, soft/hard recovery race, App Network re-establish.

## Fixed

- Android TUN health requires **both** gen204 and FCM push host (no silent green when notifications die)
- System VPN revoke → Failed UI + clearnet restored (`OnRevoke`)
- Soft `RefreshRuntime` no longer races hard `UnexpectedStop` mid-recovery
- App Network apply rebuilds VPN when bypass/IPv6 hash changes (soft refresh when possible)

## Added

- Wi‑Fi ↔ cellular network callback (throttled session recovery)
- Battery exemption re-prompt every 7 days until granted
- Signal / Slack edge push hosts

## Verify

1. Airplane toggle / Wi‑Fi↔LTE while connected — apps recover without opening v2rayF
2. System revoke VPN — status Failed, clearnet works, Connect restores
3. Force-stop WhatsApp → reopen; push path failure triggers soft refresh
4. Change App Network Direct while connected — interface rebuilds
5. Instagram Direct / Play Store regression

## Install

Tap **Update** or install from GitHub Releases.
