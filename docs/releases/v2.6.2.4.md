# v2rayF v2.6.2.4

Hotfix for false safety and silent gaps left after desktop sing-box TUN in v2.6.2.3.

## Fixed

- Kill switch allowlists **sing-box** for Hy2/TUIC/WG (was always `xray.exe`)
- Soft recovery re-arms KS after core recreate; pending retry when resume gate is busy
- Clear status when kill switch blocks reconnect
- Desktop App Network Direct/Block via sing-box `process_name`

## Changed

- Persistent Settings hint when Bypass China runs as Bypass LAN on sing-box
- Hide Secure Share / Smart Multipath on sing-box; reconnect tip for Xray-only prefs

## Verify

1. Desktop Admin + Hy2 + TUN + Kill Switch → outbound works; soft recovery restores
2. App Network Direct `chrome.exe` on Hy2 TUN → chrome goes direct
3. Bypass China on Android → Settings hint says LAN-only
4. Regression: Instagram Direct, WhatsApp push, Settings DoH live-apply

## Install

Tap **Update** or install from GitHub Releases.
