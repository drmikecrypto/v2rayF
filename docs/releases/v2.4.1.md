# v2rayF v2.4.1

Fix Play Store, Google Translate, and Instagram Direct on Android Connect.

## Fixed

- **Play Store / Translate** — TUN QUIC blocked so Chromium falls back to VPN HTTP proxy `10809`; Google domains resolve with real DNS (not FakeIP); Connect gates on HTTP proxy ready
- **Instagram Direct** — TUN stack back to **mixed** (system TCP + gVisor UDP); expanded Meta DNS/proxy exclusions

## Install

Tap **Update**. Private DNS Off → Connect → Play Store OK → force-stop Instagram once → Direct.
