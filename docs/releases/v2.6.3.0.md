# v2.6.3.0

## Why

Stabilize after the 2.6.2.14–.21 hotfix train: named Daily/Gaming assist postures (instead of burying Chromium assist), optional path diagnostics, Connected honesty for weak HTTP/TUN and OEM `SetHttpProxy`, and regression guards so D13 (sing-box classic Test All) cannot ship again. Experimental Android TUN stacks stay behind an explicit gate (gvisor remains shipping default).

## Added

- Daily / Gaming / Sentinel mode buttons (Iran/China unchanged)
- Show path diagnostics while Connected (default off)
- Dual-core regression tests + game/V2Box private scorecard template
- `Directory.Build.props`, `scripts/clean-artifacts.ps1`

## Fixed

- D12 Connected status honesty; D15 StatusSanitizer schemes/logcat; hygiene D16–D17, D20–D23

## Verify

```powershell
dotnet test src/v2rayF.Core.Tests/v2rayF.Core.Tests.csproj -c Release
```

On device: Daily → Play + Translate + IG Direct. Optional Gaming → game smoke (expect Play fail). Path diagnostics off by default.
