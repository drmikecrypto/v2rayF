# v2rayF v2.6.2.5

Android hotfix: Connect failed on every server with `ACCESS_NETWORK_STATE` SecurityException.

## Fixed

- Declared `ACCESS_NETWORK_STATE` and `ACCESS_WIFI_STATE` in the Android manifest
- Network callback registration after TUN create is best-effort (cannot `StopSelf` a good VPN)

## Verify

1. Update/install 2.6.2.5 over 2.6.2.4
2. Connect any Sentinel Reality / WS / Trojan link — must reach Connected
3. Toggle Wi‑Fi ↔ LTE while connected — recovery still works
4. Regression: Instagram Direct / WhatsApp notifications

## Install

Tap **Update** or install from GitHub Releases (update grants the new install-time permissions).
