# v2rayF v1.6.0

Live experience: one sticky auto-reconnect after an unexpected drop (without Survive/fragment poison).

## Highlights

- **Auto-reconnect once** — Settings toggle (on by default)
- Reconnect uses your saved settings only (no forced TLS fragment)

## Platform packages

Same multi-OS zips as prior releases. See [CHANGELOG.md](../CHANGELOG.md).
