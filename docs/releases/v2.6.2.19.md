# v2.6.2.19

## Why

2.6.2.14 D13 set `UseSingBoxForSpeedtest => UseSingBox`, so Android classic Test All ran on sing-box. That undid the proven **v2.2.3** fix (live Connect = sing-box TUN; classic speedtest = Xray) and reintroduced the **v2.2.2** universal Test All TIMEOUT pattern. Releases 2.6.2.15–18 patched probe budgets/DNS around that footgun; the historical cure is restoring the Xray speedtest path.

## Fixes

- `UseSingBoxForSpeedtest` → `RequiresSingBox(server)` only
- Live Connect unchanged: `PreferSingBoxOnAndroid` / `UseSingBox` still pick sing-box TUN for classic VLESS/VMess/Trojan/SS
- Hy2/TUIC/WG/anytls still use lean `SingBoxConfigBuilder.BuildSpeedtest`
- Keep 2.6.2.18 Connect SOCKS-only gate (HTTP 10809 advisory)

## Verify

```powershell
dotnet test src/v2rayF.Core.Tests/v2rayF.Core.Tests.csproj -c Release
```

On device: Test All should show ms on Reality + WS + SS (not TIMEOUT).

Sideload the platform zip from this release. Android: prefer in-app Update.
