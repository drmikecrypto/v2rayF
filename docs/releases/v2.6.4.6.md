# v2.6.4.6

## Fix

Daily assist: `graph.instagram.com` / `b-graph.facebook.com` removed from VpnService HTTP CONNECT exclusions.

MQTT / chat gateways stay excluded → TUN. Graph HTTPS rides `10809` with feed (QUIC UDP/443 block → TCP).

Symptom this targets: Direct send/receive/like OK, older messages / history scroll spinner forever.

## Scorecard

Split cells: `Instagram Direct send/receive` vs `Instagram Direct history scroll`.

## Verify

```powershell
dotnet test src/v2rayF.Core.Tests/v2rayF.Core.Tests.csproj -c Release --filter "FullyQualifiedName~MetaHttpProxy|FullyQualifiedName~GoldenMatrix"
```

Android Daily + non-SS: force-stop IG → thread → scroll older messages loads; MQTT still works; Play OK.
