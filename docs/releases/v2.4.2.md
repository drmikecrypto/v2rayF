# v2rayF v2.4.2

Emergency restore: internet after Connect on 2.4.1.

## Fixed

- **Everything offline while Connected** — Android TUN back to **gVisor** (v2.4.1 `mixed` dropped VpnService-inherited traffic; local SOCKS/HTTP probes still passed)
- Removed TUN QUIC → block route (fragile; not worth the risk)

Kept from 2.4.1: Google/Meta real DNS before FakeIP, Meta proxy exclusions, wait for HTTP `10809`.

## Install

Tap **Update**. Disconnect once → Private DNS Off → Connect. Confirm browser / YouTube / WhatsApp online.

Windows: if still offline with kill switch, Disconnect again or reboot (fail-closed firewall).
