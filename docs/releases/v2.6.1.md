# v2rayF v2.6.1

Faster Connect across Sentinel configs + hardened in-app Update.

## Faster Connect

- Smart Connect proxy-path probes run in parallel (early-exit after 3 good peers)
- Vision/REALITY rank timeout 6s; Connect health 8s / 12s
- Race CF / gstatic / Google generate_204 (first success wins)
- Last-good server Connect races ranking in the background

## Hardened Update

- Transient GitHub errors keep the Update button if an offer was already known
- Download retries + resume; SHA256 mismatch deletes bad zip
- Desktop: PID-based wait, copy retries, Exit only after updater starts
- Android: heavy work off UI thread

## Install

Tap **Update** when the button appears, or install from GitHub Releases.
