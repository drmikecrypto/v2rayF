# v2rayF v2.2.9

Fix Instagram Direct on Android (TUN DNS hijack).

## Fixed

- Feed/reels could work via VPN HTTP proxy `10809` while **Direct** stayed offline
- Android sing-box TUN now hijacks DNS (port **53/853** + `172.19.0.0/30`) into the DNS module before private-IP direct — same role as Xray `dns-out`

## Install

Tap **Update** in the app when offered (from v2.2.8). Private DNS Off → Connect → **force-stop Instagram once** → open Direct.
