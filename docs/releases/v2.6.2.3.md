# v2rayF v2.6.2.3

Desktop sing-box TUN, live Settings apply, soft-recovery hygiene, and Custom routing on the Android sing-box path.

## Fixed

- Desktop Hy2/TUIC/WG + TUN → real `auto_route` tunnel (not system-proxy-only with a false kill switch)
- Saving Settings while Connected applies DoH / Block IPv6 / routing / custom rules without manual Disconnect
- Soft recovery tears down before full reconnect; Android soft path rebuilds VPN when needed

## Added

- Sing-box Custom Direct / Proxy / Block rules
- Slack exact push hosts
- App Network Block-only soft refresh

## Changed

- Bypass China on sing-box → Bypass LAN (CN geosite deferred); tip shown once when applied

## Verify

1. Desktop Admin + Hy2/TUIC + TUN → real device tunnel
2. Connected → toggle DoH / Block IPv6 / custom direct rule → applies without Disconnect
3. Soft recovery: tun-only failure → soft refresh; if soft fails, clean reconnect
4. Android Custom Direct domain → traffic goes direct
5. Regression: Instagram Direct, WhatsApp notifications, App Network bypass

## Install

Tap **Update** or install from GitHub Releases.
